---
import { Image } from 'astro:assets';
import Base from '../layouts/Base.astro';
import Header from '../components/Header.astro';
import Icon from '../components/Icon.astro';
import LeadModal from '../components/LeadModal.astro';
import Bands from '../components/Bands.astro';
import Mono from '../components/Mono.astro';
import sala from '../assets/sala.jpg';
import cardViviane from '../assets/card-viviane.png';
import cardMarcos from '../assets/card-marcos.png';
import cardConvidado1 from '../assets/card-convidado-1.png';
import cardConvidado2 from '../assets/card-convidado-2.png';
import ticket from '../assets/ticket-lote-zero.png';

const daqui = [
  'Faturamento travado no teto',
  'Cliente que entra e sai',
  'Equipe que não anda sem você',
  'Você fazendo tudo, o dia todo',
];
const dela = [
  'Escritório lucrativo e previsível',
  'Cliente que fica e indica',
  'Time que roda sem você',
  'Você no estratégico, com liberdade',
];
const fases = [
  { icon: 'mind',  nome: 'Mentalidade & Modelo' },
  { icon: 'users', nome: 'Equipe & Estrutura' },
  { icon: 'cpu',   nome: 'Sistemas, Tech & IA' },
  { icon: 'chart', nome: 'Receita & Faturamento' },
];
const sprint = [
  { quando: 'Evento',    oque: '29–30/out' },
  { quando: 'Novembro',  oque: 'implementa' },
  { quando: 'Dezembro',  oque: 'consolida' },
  { quando: '2027',      oque: 'estruturado' },
];
const elenco = [
  { img: cardViviane,    alt: 'Viviane Araújo, fundadora da Soluzzi e mentora' },
  { img: cardMarcos,     alt: 'Marcos Paulo, Contadores Visionários' },
  { img: cardConvidado1, alt: 'Palestrante a revelar, anunciado no grupo' },
  { img: cardConvidado2, alt: 'Palestrante a revelar, anunciado no grupo' },
];
const criterios = [
  'Você é dono de escritório contábil (não estudante, não iniciante)',
  'A empresa não anda sem você, e isso já te esgotou',
  'Você tem técnica de sobra, mas gestão de menos',
  'Você prefere implementar a acumular mais um curso na estante',
  'Você quer entrar em 2027 com o escritório estruturado, não no mesmo caos',
];
const vantagens = [
  { icon: 'star',      texto: 'O menor preço do evento: abre antes de todos os lotes, por 48h, e não volta.' },
  { icon: 'gift',      texto: 'Bônus físico exclusivo pra quem garantir nas primeiras 48h.' },
  { icon: 'stopwatch', texto: 'Prioridade na abertura: você é avisado primeiro, antes da lista pública.' },
  { icon: 'clapper',   texto: 'Bastidores e lineup: palestrantes e novidades em primeira mão.' },
];
---

<Base>
  <Header />

  <!-- ══ Abertura ══ -->
  <section class="hero" id="topo">
    <div class="hero__plate">
      <Image
        src={sala} alt="Visionários ao Vivo, edição anterior, sala lotada de participantes"
        widths={[720, 1200, 1600]} sizes="100vw" loading="eager" fetchpriority="high" quality={82}
      />
      <div class="hero__wash"></div>
    </div>
    <span class="glow glow--gold hero__g1" aria-hidden="true"></span>
    <span class="glow glow--navy hero__g2" aria-hidden="true"></span>

    <div class="shell hero__in">
      <span class="badge"><Mono size={15} class="badge__mark" /> Visionários ao vivo 2026</span>

      <h1>Sua nova visão da contabilidade <span class="hand">começa agora.</span></h1>

      <p class="hero__sub">
        Dois dias de implementação mão na massa para o dono de escritório contábil
        reinventar o próprio negócio e <b>resolver 6 meses de estruturação em 2 dias.</b>
      </p>

      <button class="cta" type="button" data-open-lead>Garantir meu ingresso agora.</button>

      <ul class="facts">
        <li><Icon name="calendar" size={15} /> 29 e 30 de Outubro de 2026</li>
        <li><Icon name="pin" size={15} /> São Paulo</li>
        <li><Icon name="hourglass" size={15} /> 2 dias presenciais</li>
      </ul>
    </div>

  </section>

  <Bands />

  <div class="tail">
  <!-- ══ O muro ══ -->
  <section class="eyebrow-free" aria-labelledby="t-muro">
    <span class="glow glow--navy g-left" aria-hidden="true"></span>
    <div class="shell center measure-wide">
      <p class="over">Contador comum × Contador visionário</p>
      <h2 id="t-muro">Existe um muro <span class="grad">entre você e o escritório</span> que você queria ter.</h2>
      <p class="sec-lede measure center">
        A diferença entre os dois lados nunca foi técnica: é visão.<br class="turn" />
        O evento é o portal pra atravessar.
      </p>
    </div>

    <div class="shell cross">
      <div class="cross__side">
        <p class="cross__head"><Icon name="wall" size={16} /> <span class="grad">Do lado de cá</span></p>
        <ul>{daqui.map((t) => <li>{t}</li>)}</ul>
      </div>

      <div class="cross__seam" aria-hidden="true">
        <span class="cross__gate"><Icon name="arrow" size={20} /></span>
      </div>

      <div class="cross__side cross__side--to">
        <p class="cross__head cross__head--to"><Icon name="key" size={16} /> <span class="grad">Do outro lado</span></p>
        <ul>{dela.map((t) => <li>{t}</li>)}</ul>
      </div>
    </div>
  </section>

  <!-- ══ Método ══ -->
  <section aria-labelledby="t-metodo">
    <span class="glow glow--gold g-right" aria-hidden="true"></span>

    <div class="shell method">
      <div class="method__text">
        <p class="over">Método Aceleração Visionária · exclusivo do evento</p>
        <h2 id="t-metodo" class="h-lg">
          2 dias. 6 meses<br class="turn" />
          de <span class="hand grad">estruturação</span><br class="turn" />
          <span class="hand grad">resolvidos.</span>
        </h2>
        <p class="method__lede">
          4 fases, uma construída sobre a outra,<br class="turn" />
          e cada bloco entrega uma ferramenta pra aplicar:
        </p>
        <p class="aside">
          <Icon name="star" size={17} /> + Reforma tributária como tema transversal nos dois dias.
        </p>
      </div>

      <!-- As quatro fases como degraus: cada uma se apoia na anterior. -->
      <ol class="steps">
        {fases.map((f, i) => (
          <li style={`--h:${52 + i * 16}%`}>
            <div class="steps__slot">
              <div class="steps__bar">
                <span class="steps__n">{i + 1}</span>
                <Icon name={f.icon} size={26} />
              </div>
            </div>
            <p class="steps__label">{f.nome}</p>
          </li>
        ))}
      </ol>
    </div>

    <div class="shell">
      <div class="sprint">
        <!-- Espaço reservado: troque por <Image> quando a foto chegar. -->
        <div class="sprint__art" role="img" aria-label="Espaço reservado para foto do evento">
          <span class="sprint__ph">
            <Icon name="play" size={26} />
            <b>Foto aqui</b>
            <span>proporção 3:4</span>
          </span>
        </div>

        <div class="sprint__body">
          <h3>
            Você sai com um <span class="hand">Sprint de 60 dias</span>
            pra chegar em 2027 estruturado
          </h3>
          <ol class="vrail">
            {sprint.map((e, i) => (
              <li class={i === sprint.length - 1 ? 'vrail--now' : ''}>
                <b>{e.quando}</b>
                <span>{e.oque}</span>
              </li>
            ))}
          </ol>
        </div>
      </div>
    </div>
  </section>

  <!-- ══ Palestrantes ══ -->
  <section aria-labelledby="t-quem">
    <span class="glow glow--gold g-left" aria-hidden="true"></span>
    <div class="shell center measure-wide">
      <p class="over">Quem sobe ao palco</p>
      <h2 id="t-quem" class="grad h-lg">Os palestrantes</h2>
      <p class="sec-lede measure center">
        Viviane e Marcos conduzem o método. E, a cada semana, um novo nome de peso do
        mercado contábil é revelado.
      </p>
    </div>

    <ul class="shell cast">
      {elenco.map((e) => (
        <li class="cast__card">
          <Image src={e.img} alt={e.alt} widths={[320, 619]}
                 sizes="(max-width: 62rem) 45vw, 17rem" quality={88} />
        </li>
      ))}
    </ul>

    <div class="shell">
      <p class="aside aside--note">
        Os convidados são revelados um a um, sempre primeiro pra quem<br class="turn" />
        está na lista do Lote Zero.
      </p>
    </div>
  </section>

  <!-- ══ Qualificação ══ -->
  <section aria-labelledby="t-pra">
    <div class="shell center measure-wide">
      <p class="over">Antes de entrar</p>
      <h2 id="t-pra" class="h-lg">É pra você se…</h2>
    </div>
    <ul class="shell bento">
      {criterios.map((c) => (
        <li class="card">
          <span class="medal medal--sm"><Icon name="check" size={18} /></span>
          <p>{c}</p>
        </li>
      ))}
    </ul>
  </section>

  <!-- ══ Lote Zero ══ -->
  <section class="eyebrow-free" aria-labelledby="t-vant">
    <span class="glow glow--gold g-center" aria-hidden="true"></span>
    <div class="shell center measure-wide">
      <p class="over">Por que entrar agora</p>
      <h2 id="t-vant">A vantagem de estar na lista</h2>
    </div>

    <div class="shell lote">
      <div class="card lote__frame rise">
        <div class="lote__art">
          <Image src={ticket} alt="Ingresso Lote Zero do Visionários ao Vivo, com Viviane Araújo e Marcos Paulo"
                 widths={[430, 858]} sizes="(max-width: 60rem) 80vw, 24rem" quality={88} />
        </div>

        <div class="lote__side">
          <div class="lote__head">
            <img class="lote__logo" src="/logo-visionarios-ao-vivo.svg"
                 alt="Visionários ao Vivo" width="196" height="48" />
            <span class="tag">Lote Zero</span>
          </div>

          <ul class="perks">
            {vantagens.map((v) => (
              <li><span class="medal medal--sm"><Icon name={v.icon} size={19} /></span><span>{v.texto}</span></li>
            ))}
          </ul>

          <div class="lote__act">
            <button class="cta cta--wide" type="button" data-open-lead>
              Quero o Lote Zero + bônus <Icon name="arrow" size={18} />
            </button>

            <div class="tick" role="timer" aria-live="off">
              <span class="tick__tag">Faltam:</span>
              <div class="tick__boxes">
                <div class="tick__box"><b data-u="d">00</b><span>DIAS</span></div>
                <div class="tick__box"><b data-u="h">00</b><span>HORAS</span></div>
                <div class="tick__box"><b data-u="m">00</b><span>MIN</span></div>
                <div class="tick__box"><b data-u="s">00</b><span>SEG</span></div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ══ Fechamento ══ -->
  <section class="close eyebrow-free" aria-labelledby="t-fim">
    <span class="glow glow--navy g-center" aria-hidden="true"></span>
    <div class="shell center measure-wide">
      <p class="over">A decisão</p>
      <h2 id="t-fim">Você pode entrar em 2027 igual.<br />Ou <span class="hand">atravessar o muro</span> agora.</h2>
      <p class="sec-lede measure center">
        Entre na lista do Lote Zero e seja o primeiro a garantir sua vaga na 1ª edição do
        Visionários ao Vivo, pelo menor preço, com bônus exclusivo.
      </p>
      <button class="cta cta--wide" type="button" data-open-lead>Entrar na lista do Lote Zero</button>
    </div>
  </section>

  <footer class="foot">
    <div class="shell">Visionários ao Vivo 2026 · Contadores Visionários</div>
  </footer>
  </div>

  <LeadModal />
</Base>

<style>
  .measure-wide { max-width: 46rem; }

  /* A hero termina em preto; a dobra seguinte nasce nesse mesmo preto e só
     então caminha para o azul da página. Sem isso fica a emenda visível. */
  .tail {
    background: linear-gradient(180deg, #000001 0, #01020a 14rem, var(--ground) 34rem);
    background-repeat: no-repeat;
  }

  /* Linha curta acima do título: centralizada e destacada como rótulo do bloco,
     não empilhada como rótulo de formulário. */
  /* Proximidade: o rótulo pertence ao título, então encosta nele. */
  .over {
    margin-bottom: 0.77rem;
    font-size: 0.82rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--gold-flare);
  }

  /* ── Abertura: reconstruída a partir do arquivo do cliente ── */
  .hero {
    position: relative;
    padding-block: clamp(10rem, 27vh, 18rem) clamp(11rem, 22vw, 19rem);
    isolation: isolate;
    overflow: clip;
    text-align: center;
  }
  .hero__plate { position: absolute; inset: 0; z-index: -2; isolation: isolate; }
  .hero__plate :global(img) {
    position: relative; z-index: 0;
    width: 100%; height: 100%;
    object-fit: cover; object-position: 50% 28%;
    filter: saturate(0.9) contrast(1.02) brightness(0.86);
  }
  /* Escurecimento vertical até o preto, como no arquivo. */
  .hero__wash {
    position: absolute; inset: 0; z-index: 1;
    background:
      linear-gradient(180deg, #00000108 0%, #00000159 46%, #000001 87%),
      radial-gradient(78% 52% at 50% 46%, #0000005c 0%, #000001b3 100%);
  }
  .hero__g1 { width: 46rem; height: 28rem; top: 16%; left: 50%; transform: translateX(-50%); opacity: 0.22; }
  .hero__g2 { display: none; }

  .hero__in { position: relative; z-index: 2; }

  .badge__mark { flex: none; color: var(--gold-flare); }

  h1 {
    margin-top: 20px;
    max-width: 20ch;
    margin-inline: auto;
    font-size: clamp(2.4rem, 5.6vw, 5.125rem);
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.05em;
    color: var(--paper);
    text-shadow: 0 2px 50px #000000a6;
  }
  h1 .hand {
    font-size: 1.07em;
    font-weight: 300;
    letter-spacing: -0.01em;
  }

  .hero__sub {
    max-width: 45rem;
    margin: 20px auto 0;
    font-size: clamp(1.02rem, 2.1vw, 1.5rem);
    line-height: 1.3;
    letter-spacing: -0.03em;
    color: var(--paper);
  }
  .hero__sub b { font-weight: 700; color: var(--gold-flare); }

  .hero .cta {
    margin-top: 2.4rem;
    min-width: min(100%, 26rem);
    padding: 1.35rem 3.75rem;
    font-size: clamp(0.95rem, 1.6vw, 1.25rem);
  }

  .facts {
    display: flex; justify-content: center; flex-wrap: wrap;
    gap: 0.7rem 1.4rem;
    margin-top: 2.1rem;
    font-size: 0.82rem;
    color: #f0f0f0;
  }
  .facts li { display: inline-flex; align-items: center; gap: 5px; }
  .facts :global(svg) { color: var(--gold); flex: none; }

  /* ── Muro: tabela de travessia, conforme o nó 2722:2080 ── */
  .cross {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: clamp(1.5rem, 4vw, 3.25rem);
    width: min(100%, 58rem);
    margin: clamp(3rem, 6vw, 4.5rem) auto 0;
    align-items: stretch;
  }
  /* A quebra é decisão de leitura no desktop; no celular a linha já quebra
     sozinha e a marca atrapalharia. */
  .turn { display: none; }
  @media (min-width: 44rem) { .turn { display: inline; } }

  .cross__side { position: relative; min-width: 0; }

  .cross__head {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    margin-bottom: 1.4rem;
    font-size: 0.78rem;
    font-weight: 600;
    letter-spacing: 0.19em;
    text-transform: uppercase;
    color: var(--gold);
  }
  .cross__head .grad { font-weight: 600; }
  .cross__head :global(svg) { flex: none; }

  .cross__side ul { display: grid; }
  .cross__side li {
    padding-block: clamp(1rem, 2.2vw, 1.45rem);
    border-bottom: 1px solid #ffffff1c;
    font-size: clamp(0.98rem, 1.9vw, 1.14rem);
    color: var(--paper);
  }
  .cross__side--to li { color: var(--paper); }

  /* A divisa: filete vertical com o portal no meio. */
  .cross__seam {
    position: relative;
    width: 1px;
    background: linear-gradient(180deg, transparent, var(--gold-deep) 12%, var(--gold) 50%, var(--gold-deep) 88%, transparent);
  }
  .cross__gate {
    position: absolute;
    top: 50%;
    left: 50%;
    translate: -50% -50%;
    display: grid;
    place-items: center;
    width: 3.25rem;
    height: 3.25rem;
    border: 1px solid var(--gold);
    border-radius: 50%;
    background: #05060f;
    color: var(--gold-flare);
  }

  @media (max-width: 56rem) {
    .cross { grid-template-columns: 1fr; gap: 0; }
    .cross__seam {
      width: auto;
      height: 4.5rem;
      background: linear-gradient(90deg, transparent, var(--gold-deep) 20%, var(--gold) 50%, var(--gold-deep) 80%, transparent);
      background-size: 100% 1px;
      background-repeat: no-repeat;
      background-position: center;
    }
    .cross__gate { rotate: 90deg; }
    .cross__side--to { padding-top: 0.5rem; }
  }

  /* ── Método: texto à esquerda, degraus à direita ── */
  .method {
    display: grid;
    grid-template-columns: minmax(0, 0.85fr) minmax(0, 1fr);
    gap: clamp(2.5rem, 6vw, 5rem);
    align-items: center;
  }
  .method__text { text-align: left; }
  .method__text .over { text-align: left; }
  /* Itálica de display em regular; o .grad traz 700 e aqui ele não serve. */
  .method__text h2 .hand {
    font-weight: 400;
    font-size: 1.04em;
    letter-spacing: -0.02em;
  }
  .method__lede {
    margin-top: 1.35rem;
    color: var(--paper);
    font-size: clamp(1rem, 2.2vw, 1.12rem);
    line-height: 1.5;
  }

  .aside {
    display: flex;
    align-items: flex-start;
    gap: 0.7rem;
    max-width: 34ch;
    margin-top: 2.2rem;
    color: var(--paper);
    font-size: 0.95rem;
  }
  .aside :global(svg) { color: var(--gold); flex: none; margin-top: 0.28rem; }

  /* Degraus: a barra cresce a cada fase, e a altura vem do índice. */
  .steps {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: clamp(0.6rem, 1.4vw, 1.1rem);
    align-items: stretch;
    min-height: clamp(17rem, 30vw, 23rem);
  }
  /* As colunas têm a mesma altura e o conteúdo desce até o pé; com o rótulo
     reservando duas linhas, as barras assentam todas na mesma base. */
  .steps li { display: flex; flex-direction: column; }
  /* A altura da barra é porcentagem DESTE espaço, não da coluna inteira: assim
     a de 100% chega ao topo em vez de bater no teto junto com a de 86%. */
  .steps__slot { flex: 1; display: flex; align-items: flex-end; }

  .steps__bar {
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.55rem;
    height: var(--h);
    padding-top: clamp(1rem, 2.4vw, 1.6rem);
    border-radius: 14px 14px 4px 4px;
    color: var(--navy);
    background-image:
      linear-gradient(180deg, #ffffff40, transparent 34%),
      linear-gradient(168deg, var(--gold-flare) 0%, var(--gold) 46%, #a8863f 100%);
    box-shadow:
      inset 0 1px 0 #fff8e6cc,
      0 18px 40px -22px #d7b35666;
  }
  .steps__n {
    font-family: 'ivypresto-display', Georgia, serif;
    font-size: clamp(1.5rem, 3.4vw, 2.15rem);
    font-weight: 400;
    line-height: 1;
  }
  .steps__label {
    min-height: 2.6em;
    margin-top: 1.1rem;
    font-size: clamp(0.82rem, 1.5vw, 0.95rem);
    font-weight: 600;
    line-height: 1.3;
    color: var(--paper);
    text-align: center;
  }

  @media (max-width: 60rem) {
    .method { grid-template-columns: 1fr; }
    .steps { min-height: 15rem; }
  }
  @media (max-width: 34rem) {
    .steps { grid-template-columns: repeat(2, 1fr); gap: 1.6rem 0.8rem; min-height: 0; }
    .steps__slot { flex: none; }
    .steps__bar { height: auto; padding-bottom: 1.1rem; }
    .steps__label { min-height: 0; }
  }

  /* ── Sprint: foto à esquerda, linha do tempo vertical à direita ── */
  .sprint {
    position: relative;
    width: min(100%, 64rem);
    margin: clamp(4rem, 8vw, 6.5rem) auto 0;
    display: grid;
    grid-template-columns: minmax(0, 21rem) minmax(0, 1fr);
    gap: clamp(1.75rem, 4vw, 3.5rem);
    align-items: center;
  }

  /* Espaço reservado da foto: superfície da página, não caixa de erro. */
  .sprint__art {
    display: grid;
    place-items: center;
    aspect-ratio: 3 / 4;
    border-radius: 14px;
    border: 1px dashed var(--edge-lit);
    background:
      radial-gradient(85% 55% at 50% 40%, #c8a45c14, transparent 72%),
      linear-gradient(180deg, #0b0f2c, #04050f);
  }
  .sprint__ph {
    display: grid;
    justify-items: center;
    gap: 0.35rem;
    color: var(--paper);
    text-align: center;
  }
  .sprint__ph :global(svg) { color: var(--gold-lift); }
  .sprint__ph b { font-size: 0.95rem; font-weight: 600; letter-spacing: 0.02em; }
  .sprint__ph span { font-size: 0.8rem; letter-spacing: 0.1em; text-transform: uppercase; }

  .sprint__body { padding-right: clamp(0.25rem, 1.6vw, 1.25rem); }
  /* Menor que as headlines de seção: aqui é subtítulo de bloco, não título. */
  .sprint h3 {
    font-size: clamp(1.6rem, 2.9vw, 2.3rem);
    font-weight: 600;
    line-height: 1.16;
    letter-spacing: -0.025em;
    color: var(--paper);
    text-wrap: balance;
  }
  .sprint h3 .hand { font-size: 1.08em; }

  /* Linha do tempo vertical: um fio contínuo, quatro marcos descendo dele. */
  .vrail {
    position: relative;
    display: grid;
    gap: clamp(1.1rem, 2vw, 1.5rem);
    margin-top: clamp(1.75rem, 3.4vw, 2.5rem);
    padding-left: 1.9rem;
  }
  .vrail::before {
    content: '';
    position: absolute;
    left: 5px;
    top: 0.55em;
    bottom: 0.55em;
    width: 1px;
    background: linear-gradient(180deg, #c8a45c40, var(--gold-lift));
  }
  .vrail li { position: relative; display: flex; align-items: baseline; gap: 0.6rem; flex-wrap: wrap; }
  .vrail li::before {
    content: '';
    position: absolute;
    left: -1.9rem;
    top: 0.5em;
    width: 11px;
    height: 11px;
    border-radius: 50%;
    background: var(--gold-deep);
    box-shadow: 0 0 0 4px #05060f;
  }
  .vrail b { font-size: 1.05rem; font-weight: 700; color: var(--paper); }
  .vrail span { font-size: 0.98rem; color: var(--paper); }
  /* O último marco é onde a promessa aterrissa: é ele que acende. */
  .vrail li.vrail--now::before { background: var(--gold-flare); box-shadow: 0 0 0 4px #05060f, 0 0 0 7px #d7b35633; }
  .vrail--now span { color: var(--gold-lift); }

  @media (max-width: 62rem) {
    .sprint { grid-template-columns: 1fr; justify-items: center; }
    .sprint__art { width: min(100%, 20rem); }
    .sprint__body { padding-right: 0; width: min(100%, 30rem); }
  }

  /* ── Elenco: cada card é uma peça fechada (nó 2742:6835) ── */
  .cast {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: clamp(0.42rem, 0.9vw, 0.8rem);
    margin-top: clamp(1.7rem, 3.4vw, 2.6rem);
  }
  /* Sem borda: o PNG já traz o canto arredondado e a moldura desenhada. */
  .cast__card { line-height: 0; }
  .cast__card :global(img) {
    width: 100%;
    height: auto;
    aspect-ratio: 619 / 892;
    object-fit: cover;
  }

  @media (max-width: 62rem) {
    .cast { grid-template-columns: repeat(2, 1fr); }
  }

  /* Nota sob os palestrantes: sem ícone, duas linhas, quebra ditada. */
  .aside--note {
    display: block;
    max-width: none;
    margin-inline: auto;
    text-align: center;
  }

  /* ── Qualificação: cinco quadrados iguais, 3 + 2 ── */
  .bento {
    display: grid;
    grid-template-columns: repeat(6, 1fr);
    gap: clamp(0.42rem, 0.9vw, 0.7rem);
    width: min(100%, 60rem);
    margin: clamp(3rem, 6vw, 4.5rem) auto 0;
  }
  /* Fundo: o gradiente dourado da marca a 5% sobre a base escura. */
  .bento li {
    position: relative;
    grid-column: span 2;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    gap: clamp(1.1rem, 2.2vw, 1.5rem);
    padding: clamp(1.6rem, 3vw, 2.2rem) clamp(1.4rem, 2.6vw, 1.9rem);
    overflow: hidden;
    background:
      linear-gradient(140deg, #d7b3560d 0%, #f4edd00d 52%, #f4edd000 100%),
      linear-gradient(180deg, #080b23, #04050f);
    transition:
      transform 0.45s var(--ease),
      box-shadow 0.45s var(--ease),
      border-color 0.5s var(--ease);
  }
  /* Os dois de baixo ficam centrados sob os três de cima. */
  .bento li:nth-child(4) { grid-column: 2 / span 2; }
  .bento li:nth-child(5) { grid-column: 4 / span 2; }

  /* Luz que sobe do pé da caixa ao passar o mouse. */
  .bento li::after {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: inherit;
    background: radial-gradient(115% 72% at 50% 116%, #d7b3564d, #d7b35614 38%, transparent 66%);
    opacity: 0;
    transition: opacity 0.55s var(--ease);
    pointer-events: none;
  }
  .bento li:hover {
    border-color: #fad49f80;
    box-shadow: 0 26px 50px -22px #000000d9;
  }
  .bento li:hover::after { opacity: 1; }
  @media (prefers-reduced-motion: no-preference) {
    .bento li:hover { transform: translateY(-6px); }
  }

  .bento p {
    position: relative;
    z-index: 1;
    font-size: clamp(1rem, 1.9vw, 1.12rem);
    font-weight: 700;
    line-height: 1.42;
    letter-spacing: -0.012em;
    color: var(--paper);
  }
  .bento .medal { position: relative; z-index: 1; }

  @media (max-width: 62rem) {
    .bento { grid-template-columns: repeat(2, 1fr); }
    .bento li,
    .bento li:nth-child(4) { grid-column: span 1; }
    /* Sobrando um, ele fica centrado na última fileira. */
    .bento li:nth-child(5) { grid-column: span 1; }
  }
  @media (max-width: 40rem) {
    .bento { grid-template-columns: 1fr; }
  }

  /* ── Lote ── */
  /* ── Lote Zero: o ingresso à esquerda, o argumento à direita ── */
  .lote { margin-top: 3rem; max-width: 64rem; }
  .lote__frame {
    display: grid;
    grid-template-columns: minmax(0, 24rem) minmax(0, 1fr);
    gap: clamp(1.5rem, 3vw, 2.6rem);
    padding: clamp(1.1rem, 2.2vw, 1.6rem);
    align-items: stretch;
    background: linear-gradient(180deg, #000001, #04050f);
  }
  /* A arte já é o ingresso inteiro: logo, chips e código de barras vêm nela.
     No formato alto ela cobre a altura da coluna sozinha, sem placa. */
  .lote__art { line-height: 0; }
  /* Sem raio aqui: o canto arredondado já vem no PNG. */
  .lote__art :global(img) { width: 100%; height: auto; }

  /* O conteúdo é mais baixo que o ingresso: centrado, o resto vira margem
     em cima e embaixo em vez de um buraco só no pé. */
  .lote__side {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding-right: clamp(0.2rem, 1.6vw, 1.2rem);
  }
  .lote__head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.25rem;
    flex-wrap: wrap;
  }
  .lote__logo { width: 172px; height: 42px; }

  /* Etiqueta do lote, no lugar da marca do produto na referência. */
  .tag {
    display: inline-flex;
    align-items: center;
    padding: 0.5rem 1.15rem;
    border-radius: 100px;
    background-image: linear-gradient(90deg, var(--gold) 0%, var(--gold-flare) 100%);
    color: var(--navy);
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  /* Os filetes que separam os blocos vêm da referência. */
  .perks {
    margin-top: 1.4rem;
    padding-top: 1.4rem;
    border-top: 1px solid var(--edge-lit);
    display: grid;
    gap: 1.1rem;
  }
  /* Ícone centrado na altura do texto, não colado no topo. */
  .perks li { display: grid; grid-template-columns: auto 1fr; gap: 0.9rem; align-items: center; color: var(--paper); }
  /* Medida curta de propósito: com a largura toda, um item cabe em uma linha
     e outro sobra uma palavra sozinha. Em 40ch os quatro caem em duas linhas,
     e o balance reparte cada par para não deixar viúva. */
  .perks li > span:last-child { max-width: 40ch; text-wrap: balance; }
  .medal--sm { width: 2.4rem; height: 2.4rem; border-radius: 10px; }

  .lote__act {
    margin-top: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--edge-lit);
    text-align: center;
  }
  .lote__act .cta { width: 100%; }

  /* ── Regressivo compacto sob a ação ── */
  .tick {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.7rem;
    margin-top: 1.1rem;
    flex-wrap: wrap;
  }
  .tick__tag {
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--paper);
  }
  .tick__boxes { display: flex; gap: 7px; }
  .tick__box {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 2px;
    min-width: 66px;
    padding: 0.7rem 0.85rem;
    border: 1px solid var(--edge-lit);
    border-radius: 6px;
    background: #5a3e361a;
    color: var(--paper);
  }
  .tick__box b { font-size: 1.25rem; font-weight: 700; line-height: 1.2; font-variant-numeric: tabular-nums; }
  .tick__box span { font-size: 9px; font-weight: 400; line-height: 1.2; color: #ffffffb3; }

  @media (max-width: 60rem) {
    .lote__frame { grid-template-columns: 1fr; }
    .lote__art { max-width: 20rem; margin-inline: auto; }
  }

  /* ── Fechamento ── */
  .close { padding-bottom: clamp(5.5rem, 11vw, 9rem); }
  .close .cta { margin-top: 2.6rem; }

  /* Halos de seção */
  .g-left   { width: 34rem; height: 24rem; top: 6%; left: -14rem; opacity: 0.32; }
  .g-right  { width: 34rem; height: 24rem; top: 10%; right: -14rem; opacity: 0.28; }
  .g-center { width: 42rem; height: 26rem; top: 18%; left: 50%; transform: translateX(-50%); opacity: 0.3; }

  .foot {
    padding-block: 2.4rem;
    color: var(--paper); font-size: 0.82rem; text-align: center;
    box-shadow: inset 0 1px 0 #ffffff0d;
  }
</style>
